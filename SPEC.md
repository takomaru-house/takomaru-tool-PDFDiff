# PDF差分検出ツール 仕様書

最終更新: 2026-05-16

本ドキュメントは、ブラウザ上で動作するPDF差分検出ツールの仕様および実装計画である。Claude Code への実装依頼を前提とし、技術選定・要件・スプリント計画を含む。

---

## 1. プロジェクト概要

### 1.1 目的
2つのPDFファイル（および画像ファイル）を比較し、差分箇所を視覚的・テキスト的に把握できるツールをブラウザ上で提供する。

### 1.2 想定ユーザー
- ドキュメントの版管理を行う担当者（仕様書、契約書、図面など）
- レビュー前後の変更点を素早く確認したいレビュアー

### 1.3 スコープ
- **インスコープ**: ブラウザ完結のPDF/画像比較、差分の視覚化、差分結果のエクスポート
- **アウトオブスコープ**: サーバー側処理、課金APIの利用、OCR（初期版）、3者以上の比較

---

## 2. 機能要件

### 2.1 入力
- ユーザーは2つのPDFファイルをブラウザ上で選択する（旧版＝Before、新版＝After）。
- ドラッグ＆ドロップ、ファイル選択ダイアログの両方に対応する。
- 拡張仕様として、画像ファイル（PNG / JPEG / WebP）も入力可能とする。
- ファイルはローカル処理のみ。サーバーには送信しない（プライバシー要件）。

### 2.2 差分検出
PDFについては以下2種類の差分を併用する。

1. **テキスト差分**: pdf.jsで抽出したテキストとその座標から、追加／削除／変更を検出。
2. **視覚（ピクセル）差分**: PDFを高解像度でCanvasにレンダリングし、ピクセル単位で比較。図表・レイアウト変更を検出。

画像ファイルは視覚（ピクセル）差分のみを実施する。

### 2.3 差分の表示
- **差分リスト**: 画面サイドバーに、ページ・差分種別（追加/削除/変更）・テキスト概要を一覧表示。
- **差分ハイライト**: 差分箇所をPDFビューア上で色付け表示する（例: 追加=緑、削除=赤、変更=黄）。
- **重ね合わせ表示**: BeforeとAfterを重ね合わせ、透過率（0〜100%）をスライダーで調整可能。
- **連動フォーカス**: 差分リストの項目をクリックすると、該当箇所へ自動スクロール／ズームし、その箇所のハイライト色や枠線が一時的に強調される。

### 2.4 表示モード
ユーザーが切り替え可能な3モードを提供する。

| モード | 内容 |
|---|---|
| 並列表示 (Side-by-Side) | 左にBefore、右にAfter。同期スクロールで比較しやすくする。 |
| 重ね合わせ表示 (Overlay) | 同位置に重ね、透過率スライダーで切替。差分箇所のみハイライト。 |
| 差分のみ (Diff Only) | 差分が検出されたマスクのみ表示。変更点の全体像を把握しやすくする。 |

### 2.5 ページ・複数ページ対応
- 複数ページPDFに対応する。
- ページ送りUI（前/次/ジャンプ）を備える。
- 差分リストはページ単位でグルーピングする。

### 2.6 結果出力（エクスポート）
- **HTMLレポート**: ハイライト画像と差分リストを含む単一HTMLファイル。
- **JSON**: 差分データ（ページ・座標・種別・テキスト）。
- **注釈付きPDF（任意・後回し可）**: After PDFに差分の注釈・ハイライトを焼き込んだPDFを出力。

### 2.7 拡張仕様: 画像ファイル対応
- 入力に画像ファイルを許可し、視覚差分のみを実施する。
- PDF処理パスとUIをできるだけ共通化する（後述のアーキテクチャ参照）。

---

## 3. 非機能要件

| 区分 | 要件 |
|---|---|
| 動作環境 | 最新の Chrome / Edge / Firefox / Safari。スマホ(iOS Safari / Android Chrome)にも対応(§14)。 |
| パフォーマンス | A4・10ページ程度のPDFで30秒以内に差分表示が完了すること。 |
| プライバシー | ファイルは外部送信しない。完全クライアントサイド処理。 |
| コスト | 課金APIを使わない。OSSライブラリのみ利用。 |
| 配布 | 初期はローカル静的ホスティング。最終的に GitHub Pages / Vercel / Netlify の無料枠で公開可能とする。 |
| ライセンス | 採用するOSSのライセンス（MIT / Apache-2.0 / BSDなど）を README に明記する。 |

---

## 4. 技術選定

すべて無料・OSSのみで構成する。バックエンド不要のフロントエンド単体アプリ。

### 4.1 採用ライブラリ
| 用途 | ライブラリ | ライセンス | 備考 |
|---|---|---|---|
| ビルド・開発環境 | Vite | MIT | 高速HMR |
| UIフレームワーク | React 18 + TypeScript | MIT | コンポーネント分割しやすい |
| スタイル | Tailwind CSS | MIT | shadcn/ui 併用想定 |
| PDFレンダリング・テキスト抽出 | **pdf.js (Mozilla)** | Apache-2.0 | 業界標準 |
| ピクセル差分 | **pixelmatch** | ISC | Mapbox製、軽量 |
| テキスト差分 | **diff-match-patch (Google)** | Apache-2.0 | 文字単位差分が高精度 |
| 注釈付きPDF出力（拡張） | pdf-lib | MIT | PDFを直接編集可能 |
| 状態管理 | Zustand | MIT | 軽量、Reducer不要 |

### 4.2 採用しない選択肢と理由
- **PDF.NET / Adobe API / DocRaptor**: 課金、または商用ライセンス。今回の要件（無料・ローカル）に反する。
- **Tesseract.js（OCR）**: 大きく重い。スキャンPDFは初期版では非対応とし、必要時に追加検討。

### 4.3 デプロイ先候補
- ローカル: `npm run dev` で起動。
- 公開: **GitHub Pages**（静的のみで完結するため最適）または Vercel / Netlify。

---

## 5. アーキテクチャ

### 5.1 全体構成
```
[ブラウザ]
  └─ React UI
        ├─ FileInput      (PDF/画像 入力)
        ├─ DiffEngine     (差分計算ロジック)
        │    ├─ PdfAdapter   : PDF→Canvas+TextItem
        │    ├─ ImageAdapter : 画像→Canvas
        │    ├─ TextDiffer   : diff-match-patch
        │    └─ PixelDiffer  : pixelmatch
        ├─ Viewer         (3モード切替: Side-by-Side / Overlay / Diff)
        ├─ DiffList       (差分一覧 + 連動フォーカス)
        └─ Exporter       (HTML / JSON / PDF出力)
```

### 5.2 PDFと画像の共通化方針
PDF も画像も、最終的には「Canvas に描画された RasterPage」と「TextLayer（PDFのみ）」の二層に正規化する。
DiffEngine 以降は両方を区別なく扱える設計にする。

```ts
type RasterPage = { width: number; height: number; canvas: OffscreenCanvas; pageIndex: number };
type TextLayer  = { items: { text: string; bbox: [number,number,number,number] }[] };
type DocSource  = { type: 'pdf' | 'image'; pages: RasterPage[]; texts?: TextLayer[] };
```

### 5.3 差分計算の流れ
1. 双方のドキュメントを `DocSource` に変換。
2. ページ数を揃える（不足側は「空ページ」扱い）。
3. ページごとに：
   - **テキスト差分** (PDFのみ): 抽出テキストを `diff-match-patch` で比較。差分テキストの座標は、抽出時のbboxから取得する。
   - **ピクセル差分**: 解像度を揃えたCanvas同士を `pixelmatch` で比較し、差分マスク画像とバウンディングボックス群を得る。
4. 上記2種を統合し、「差分エントリ配列」を生成する。

```ts
type DiffEntry = {
  id: string;
  page: number;
  kind: 'add' | 'remove' | 'change' | 'visual';
  bbox: [x, y, w, h];      // After座標系
  beforeText?: string;
  afterText?: string;
  thumbnail?: string;       // 切り出した小画像（任意）
};
```

### 5.4 重ね合わせ表示の実装
- 同一座標系のCanvasを2枚スタック。
- After Canvasの `globalAlpha` をスライダー値で制御。
- 差分箇所のハイライトは、別レイヤー（SVGオーバーレイ）で描画し、リストクリック時に該当要素の枠を強調アニメーションさせる。

---

## 6. UI/UX 設計（ワイヤー）

```
┌─────────────────────────────────────────────────────────────────┐
│ Header: ロゴ / ファイル選択(Before/After) / モード切替 / Export   │
├──────────────┬──────────────────────────────────────────────────┤
│ DiffList     │  Viewer Area                                      │
│ ──────────── │   ┌──────────┬──────────┐  or Overlay            │
│ Page 1       │   │ Before   │  After   │   透過率 [─────●──]     │
│  ・[追加] …  │   │          │          │                         │
│  ・[削除] …  │   │          │          │                         │
│ Page 2       │   └──────────┴──────────┘                         │
│  ・[変更] …  │   Page 1 / 10  ◀ ▶                                 │
└──────────────┴──────────────────────────────────────────────────┘
```

### 主なインタラクション
- ファイル選択時、即座に処理開始（プログレス表示）。
- 差分リスト項目クリック → 該当ページへジャンプ＋該当領域に枠線アニメーション。
- 透過率スライダー（Overlayモード時のみ表示）。
- キーボードショートカット: `←/→` でページ送り、`J/K` で差分送り。

---

## 7. 受入基準 (Definition of Done)

各機能は以下を満たすこと。
- 主要ブラウザ（Chrome / Edge / Firefox）で動作する。
- 10ページのテストPDFで動作確認済み。
- ESLint / Prettier / TypeScript 型エラーなし。
- 主要ロジック（DiffEngine）は単体テスト（Vitest）が整備されている。
- README に起動手順と仕様サマリが記載されている。

---

## 8. リポジトリ構成（提案）

```
50-pdf差分検出ツール/
├─ SPEC.md                   ← 本ドキュメント
├─ README.md
├─ package.json
├─ vite.config.ts
├─ tsconfig.json
├─ index.html
├─ src/
│   ├─ main.tsx
│   ├─ App.tsx
│   ├─ components/
│   │    ├─ FileInput/
│   │    ├─ Viewer/
│   │    ├─ DiffList/
│   │    └─ Exporter/
│   ├─ engine/
│   │    ├─ PdfAdapter.ts
│   │    ├─ ImageAdapter.ts
│   │    ├─ TextDiffer.ts
│   │    ├─ PixelDiffer.ts
│   │    └─ DiffEngine.ts
│   ├─ store/                ← Zustand
│   ├─ types/
│   └─ utils/
├─ public/
│   └─ samples/              ← 動作確認用サンプルPDF
└─ tests/
```

---

## 9. スプリント計画

各スプリントは1〜2週間を想定。Claude Code への依頼粒度として、スプリント単位、もしくはタスク単位で渡せる構成にする。
各スプリント末に動作するインクリメントを残すこと（リリース可能性は問わない）。

### Sprint 0: 環境構築（目安: 3〜5日）
**ゴール**: 空のSPAが起動する。
- [ ] Vite + React + TypeScript プロジェクト雛形
- [ ] Tailwind CSS / ESLint / Prettier / Vitest 設定
- [ ] 基本レイアウト（Header / Sidebar / Main）
- [ ] CI（GitHub Actions: lint + typecheck + test）
- [ ] README雛形

**完了条件**: `npm run dev` で空のレイアウトが表示される。CI緑。

---

### Sprint 1: PDF入出力と表示（目安: 1〜2週間）
**ゴール**: 2つのPDFを読み込み、並列表示できる。
- [ ] FileInput コンポーネント（D&D + ダイアログ）
- [ ] pdf.js 統合 → `PdfAdapter`（Canvas描画 / テキスト抽出）
- [ ] Viewer の「並列表示モード」実装
- [ ] ページ送り UI と同期スクロール
- [ ] サンプルPDFを `public/samples/` に同梱

**完了条件**: 2つのPDFを選んでページめくりできる。テキスト抽出のユニットテスト合格。

---

### Sprint 2: 視覚（ピクセル）差分検出（目安: 2週間）
**ゴール**: ピクセル差分が色付きで表示される。
- [ ] `PixelDiffer`（pixelmatch ラップ、解像度合わせ）
- [ ] バウンディングボックス抽出（連結領域抽出）
- [ ] ハイライトオーバーレイ（SVG）
- [ ] 差分リスト（視覚差分のみ）

**完了条件**: 軽微な変更PDFで差分箇所が枠線表示される。10ページPDFで30秒以内処理。

---

### Sprint 3: テキスト差分検出（目安: 2週間）
**ゴール**: 追加/削除/変更がテキストで一覧化される。
- [ ] `TextDiffer`（diff-match-patch ラップ）
- [ ] テキスト座標へのマッピング（抽出item.bbox利用）
- [ ] 差分種別の分類ロジック（add/remove/change）
- [ ] 差分リストUI（ページグループ + 種別バッジ + プレビュー）

**完了条件**: テスト用PDFで日本語/英語混在の差分が正しく分類される。

---

### Sprint 4: 重ね合わせ表示と連動フォーカス（目安: 1〜2週間）
**ゴール**: Overlay モードと差分リスト連動が完成。
- [ ] Overlay モード（透過率スライダー）
- [ ] 差分リスト→該当箇所へのスクロール／ズーム
- [ ] フォーカス時の枠線アニメーション
- [ ] Diff Only モード
- [ ] キーボードショートカット（J/K, ←/→）

**完了条件**: リスト操作で該当差分が画面中央に表示され、視認しやすい。

---

### Sprint 5: 差分結果エクスポート（目安: 1週間）
**ゴール**: 差分結果を3形式で出力できる。
- [ ] HTMLレポート（単一ファイル、ハイライト画像inline）
- [ ] JSON エクスポート
- [ ] （任意）pdf-lib で注釈付きPDF出力

**完了条件**: 出力ファイルを開き直して内容が確認できる。

---

### Sprint 6: 拡張仕様 — 画像ファイル対応（目安: 1週間）
**ゴール**: PNG / JPEG / WebP を比較できる。
- [ ] `ImageAdapter` 実装
- [ ] 入力UIをPDF/画像兼用に拡張
- [ ] 画像時は視覚差分のみを実施し、UIから「テキスト差分」タブを非表示にする

**完了条件**: 画像入力で差分が表示・エクスポートできる。

---

### Sprint 7: デプロイ準備と仕上げ（目安: 1週間）
**ゴール**: Web に公開できる状態にする。
- [ ] GitHub Pages / Vercel デプロイ設定
- [ ] バンドルサイズ最適化（pdf.js worker分離など）
- [ ] パフォーマンス計測と改善（大ページPDF）
- [ ] README整備（スクリーンショット、使い方、ライセンス一覧）
- [ ] サンプルPDF/画像でのE2E動作確認

**完了条件**: 公開URLで動作する。READMEに導入手順とライセンス記載あり。

---

## 10. 将来拡張（バックログ）
- スキャンPDFのOCR対応（Tesseract.js）。
- 複数ファイルの一括比較。
- 差分結果へのコメント付与。
- ダークモード。
- 差分の重大度フィルタ（軽微な空白差分の除外など）。
- 印刷向けレポート（A4）。

---

## 11. Claude Code への依頼テンプレ

スプリント単位で依頼する場合、以下のテンプレを使うとスムーズ。

```
このリポジトリで Sprint N を実装してください。
- 仕様: SPEC.md の「Sprint N: …」セクションに従う
- 完了条件: 同セクションの「完了条件」を満たすこと
- 既存コードの規約: ESLint / Prettier / TypeScript strict
- テスト: 主要ロジックにVitest単体テストを追加
- PR は機能ごとに分割し、各コミットメッセージは Conventional Commits に従う
```

---

## 12. オープン項目（要確認）
本仕様は以下の前提を置いた。Claude Code 着手前に必要なら確定したい項目。
1. UI言語: 日本語のみ／英語併記、どちらにするか（現状は日本語想定）。
2. PDFの最大想定サイズ・ページ数（性能設計に影響）。
3. Sprint 5 の「注釈付きPDF出力」を必須機能にするか、任意のままにするか。
4. 公開先（GitHub Pages / Vercel / Netlify）の最終決定。

---

## 13. 追加仕様: 差分のレビュー状態（チェック機能）

### 13.1 目的・ユースケース
レビュアーが差分一覧を上から潰していく作業を支援する。「目視で確認済み（＝OK）」の項目にチェックを付け、未確認の差分のみをレポート出力できるようにする。100件の差分のうち30件だけが残課題、という状況を整理しやすくする。

### 13.2 実現可能性の判断
**結論: 実現可能。実装難易度は低。**

根拠:
- 既存実装は完全にクライアントサイド (`pdfDiff.js`) で動作し、差分データは `state.diffsByPage[].textDiffs[]` というメモリ上の配列に集約されている。各エントリにフラグを1つ追加するだけで状態管理は完結する。
- 差分リストUIは `renderDiffList()` (pdfDiff.js:412) で生成しており、`.diff-item` 1要素ごとにチェックボックスを差し込む拡張は局所改修で済む。
- レポート出力 (`exportHtmlReport`, pdfDiff.js:766 / JSONエクスポート, pdfDiff.js:748) はいずれも `state.diffsByPage` を起点に組み立てているため、出力前にフィルタ関数を1段挟むだけで「未チェック差分のみのレポート」が作れる。
- 外部ライブラリ追加・サーバ通信は一切不要。

### 13.3 機能要件

#### 13.3.1 データモデルの拡張
各差分エントリ `textDiffs[i]` に以下のフィールドを追加する。

```ts
type DiffEntry = {
  // 既存フィールド
  regionId: string;
  kind: 'add' | 'remove' | 'change' | 'visual';
  bbox: [x, y, w, h];
  before: string;
  after: string;
  // 追加フィールド
  checked: boolean;       // 既定値 false。レビュアーが OK とした差分は true。
  checkedAt?: number;     // 任意。チェックを付けた時刻 (Date.now())。レポートに表示しても良い。
};
```

差分エントリの一意キーは `(pageIndex, regionId)` の組とする（既存のフォーカス処理と同じキー）。

#### 13.3.2 UI要件

**差分リスト（サイドバー）**

- 各 `.diff-item` の左端にチェックボックスを追加する。レイアウトは `grid-template-columns: [checkbox] [badge] [meta]`（既存の `56px 1fr` を `auto 56px 1fr` などに変更）。
- チェックボックスのクリックは行クリック（フォーカスジャンプ）と独立させる。`event.stopPropagation()` でバブリングを止め、チェック操作だけが走るようにする。
- チェック済みの `.diff-item` は視覚的にデエンファサイズする（背景を淡くする／テキストに `opacity: 0.55`／取り消し線は付けない — 元データの取り消し線と衝突するため）。
- ページグループの見出し (`.diff-page-title`) に「3 / 12 checked」のような進捗表記を追加する。
- 全ページ集計の `#diffStats` には既存の `N ITEMS · M PG` に加えて `· K CHECKED` を併記する。

**一括操作**

- ページグループ見出し横に「✓ このページを全てチェック」「□ 解除」のミニボタンを置く。
- サイドバーヘッダー（`.sidebar-header` の下、`#diffList` の上）に「全件チェック / 全件解除 / チェックのみ非表示」のツールバー行を追加する。
- 「チェックのみ非表示」を ON にすると、`.diff-item.checked` を `display: none` にする（CSSクラスの切替で実現）。状態は `state.hideChecked` に保持する。

**キーボードショートカット**

- 既存の J/K（上下移動）に加えて、選択中の差分で **Space** を押すとチェックをトグルする。
- 入力欄にフォーカスがある場合は無効（既存 `keydown` ハンドラと同じガードを再利用）。

**ページ送り UI への補助表示**

- `#pageLabel` の補助として、現在ページの「未チェック残数」を `#footerBar` の `#status` などに簡易表示する（任意）。

#### 13.3.3 永続化方針
- 既定では「比較を実行（COMPARE）」を再度押すと `state.diffsByPage` は再生成されるため、チェック状態は破棄される（再計算で差分の数や regionId が変わり得るため）。これは仕様とする。
- localStorage への保存は **初期版ではスコープ外**。将来拡張として 13.6 に記載。

#### 13.3.4 レポート機能の拡張
ヘッダーの `レポート — REPORT` ボタンの挙動を以下に拡張する。

**A. UI: スプリットボタン方式**
従来の操作感を維持するため、**主ボタン＋ドロップダウントリガー** の構成とする。主ボタンを 1 クリックすると既定動作（＝全差分レポート）が即実行される。隣接する `▼` 部分をクリックするとメニューが開き、他の出力種別を選べる。

```
┌─────────────────────┬───┐
│ レポート — REPORT   │ ▼ │
└─────────────────────┴───┘
                       │
                       └─ ① 全差分レポート（FULL） ← 既定／主ボタンと同じ
                          ② 未チェック差分のみ（UNRESOLVED）
                          ③ 両方を1ファイルに（COMBINED, 任意）
```

- 主ボタン（左半分）クリック: 既定動作の **全差分レポート** を即時出力。これにより従来ユーザーは 1 クリックで従来通りの結果を得られる。
- ドロップダウントリガー（右の `▼`）クリック: メニューを開く。メニュー内 ① を選んでも主ボタンと同じ結果。
- 既定動作は **常に「全差分レポート」を維持** する（ユーザーが直近に選んだ種別を覚えない）。理由: 「いつもの動作」を1クリックで保証するため。覚える方式にすると、たまに使う人が想定外の出力をしてしまう恐れがある。
- 実装は `<details>`/`<summary>` を使う簡易方式でも、または `<button>` 2 個を隣接配置（主ボタン + キャレットボタン）でも良い。後者のほうが既存の `.btn.fill` スタイルと馴染む。
- メニューを開いた状態で外側をクリック or `Esc` で閉じる。

出力種別の内容は以下。

- `① 全差分レポート（FULL）` — 従来と同じ全差分入りHTML。各項目に「☑ CHECKED」「☐ UNRESOLVED」のバッジを追加。
- `② 未チェック差分のみ（UNRESOLVED）` — `checked === false` の差分のみを含むHTML。
- `③ 両方を1ファイルに（COMBINED, 任意）` — 1つのHTML内に「未チェック」「チェック済み」のセクションを順に並べる。**初期実装ではスコープ外**（メニュー項目自体を出さない）。要望が出たら追加する。

JSONエクスポート (`JSON — 出力`) は出力種別を切り替えず、全データに `checked` フィールドを含めて出す（消費側でフィルタ可能）。

**B. レポートの差分**
- ① 全差分レポート: 各差分項目に「☑ CHECKED」「☐ UNRESOLVED」のバッジを追加する。タイトル直下に「12 件中 3 件未チェック」のサマリ行を追加する。
- ② 未チェック差分のみ: タイトルを `PDF差分レポート（未チェック差分）` に変更。チェック済み差分は完全に除外。ページ内の差分がすべてチェック済みの場合、そのページのセクション自体を出力しない。差分マスク画像 (`p.diffCanvas`) は、未チェック差分が1件以上残るページのみ出力する。
- ヘッダーに生成時刻に加えて `総差分 N 件 / チェック済み K 件 / 未チェック N-K 件` を記す。

**C. レポートの内部構造（共通）**
出力ロジックは以下のように分離する。

```ts
function buildReportHtml(diffsByPage, opts: { mode: 'full' | 'unresolved' }): string {
  const filtered = filterForReport(diffsByPage, opts.mode);
  // ... 既存の組み立てロジックを再利用
}
function filterForReport(diffsByPage, mode) {
  if (mode === 'full') return diffsByPage;
  return diffsByPage
    .map(p => ({ ...p, textDiffs: p.textDiffs.filter(d => !d.checked) }))
    .filter(p => p.textDiffs.length > 0 || p.missing);
}
```

### 13.4 実装影響範囲

| 改修対象 | 内容 |
|---|---|
| `pdfDiff.js` `runDiff()` (L209) | 差分生成時に `checked: false` を初期化。 |
| `pdfDiff.js` `renderDiffList()` (L412) | チェックボックス挿入、進捗表示、一括操作ボタン。 |
| `pdfDiff.js` `focusDiff()` (L582) | チェックボックス領域のクリック判定除外（イベント側で対処）。 |
| `pdfDiff.js` `keydown` ハンドラ (L664) | Space キーでトグル。 |
| `pdfDiff.js` `$('#btnExport')` (L748) | JSON出力に `checked` フィールド追加。 |
| `pdfDiff.js` `exportHtmlReport()` (L766) | フィルタ関数導入、出力種別の切替。 |
| `index.html` | レポートボタンをスプリットボタン化（主ボタン＝全差分レポート、`▼` でメニュー）、サイドバーに一括操作行を追加。 |
| `pdfDiff.css` | `.diff-item` を3カラム grid に変更、`.diff-item.checked` のスタイル、`.diff-check` 入力のスタイル。 |

既存の `regionId` ベースのフォーカス・ハイライト処理は変更不要。

### 13.5 受入基準（追加分）
- 差分一覧の各行にチェックボックスが表示され、ON/OFF を切り替えられる。
- チェック操作で行のスタイルが変わり、差分のフォーカスジャンプは発生しない。
- ページグループ・全体サマリの「checked」カウンタが正しく追随する。
- 「全差分レポート」を出すと、従来の全差分が含まれ、各項目にチェック状態バッジが付く。
- 「未チェック差分のみレポート」を出すと、`checked: true` の差分が一切含まれない HTML が出力される。
- JSON 出力に `checked` フィールドが含まれる。
- COMPARE を再実行するとチェック状態がリセットされる（既知の仕様）。
- Chrome / Edge / Firefox で動作する。

### 13.6 将来拡張（バックログ追加）
- チェック状態を localStorage に保存し、同じファイル名（または PDF のハッシュ）を再読込した際に復元する。
- 「対応者」「コメント」フィールドの追加（チェック時に短い注記を残せる）。
- レポート出力に「コメント」「対応日時」列を含める。
- CSV 出力（チェック状況を表計算ソフトで管理したい場合）。

---

## 14. 追加仕様: モバイル(スマホ)対応

### 14.1 目的
当初は「スマホ非対応で良い」としていたが、レビュアーが外出先で確認するケースが想定されたため、レスポンシブ対応とタッチ操作対応を追加した。

### 14.2 対応端末・前提
- 幅 ≤ 820px をモバイルレイアウトとして扱う(タブレット縦持ちまでカバー)。
- 幅 ≤ 480px を小型スマホレイアウトとしてさらに最適化。
- iOS Safari (15+), Android Chrome (最新2世代) を主対象。
- 動的ビューポート (`100dvh`) を使用してアドレスバー伸縮に追従。

### 14.3 レイアウト方針 — 縦2段スプリット
モバイルでは画面が狭くサイドバーをドロワーで覆い被せるとプレビューが見えなくなるため、**縦2段スプリット**を採用する。

```
┌─────────────────────────────┐
│ Header (compact, 1〜2 row)   │
├─────────────────────────────┤
│ Toolbar (mode / zoom)       │
├─────────────────────────────┤
│                             │
│  ┌──────────────────────┐   │
│  │ PDFビュー (main)      │   │ ← 残り全部 (1fr)
│  │                       │   │
│  └──────────────────────┘   │
├─────────────────────────────┤
│ § INDEX · stats             │
│ ● 03 P.2 「金額...」        │ ← 索引(aside)
│ ● 04 P.3 「サイズ...」      │   clamp(180px, 35vh, 320px)
├─────────────────────────────┤
│ ◀ 2/12 ▶          ready     │ ← Footer
└─────────────────────────────┘
```

- 索引(aside)の高さは `clamp(180px, 35vh, 320px)`。横長端末でも縦長端末でも極端な比率にならないよう範囲を制限。
- 比較前(空状態)は索引パネルを `display: none` にし、使い方説明(`#empty`)を全画面表示する。
- 索引内のヘッダは「§ INDEX · N ITEMS · M PG · K CHECKED」の1行のみに圧縮(タイトル "Differences" と日本語サブは非表示)。

### 14.4 ヘッダの動的圧縮 — `has-pages` / `show-controls`
比較の進行段階に応じてヘッダUIを切り替える。状態は body の class で表現する。

| 状態 | body class | ファイル選択行 | 比較範囲バー | 出力ボタン |
|---|---|---|---|---|
| 起動直後 / ファイル未選択 | (なし) | 表示 | 非表示 | 表示 |
| ファイル選択済み・未比較 | (なし) | 表示 | 表示 | 表示 |
| 比較済み | `has-pages` | **非表示** | **非表示** | 表示(縮小) |
| 比較済み + 編集モード | `has-pages show-controls` | 表示 | 表示 | 表示(縮小) |

- 比較完了時に JS が `document.body.classList.add('has-pages')` を付与し、CSS でファイル選択行(`.file-row`)と比較範囲バー(`#compareBar`)を `display: none` にする。これにより iPhone 14 のような端末でステージ領域が約 2 倍に拡大する。
- ヘッダ右に「✎ 編集」ボタンを置く(モバイル+比較済みのときだけ表示)。タップすると `show-controls` クラスが付き、ファイル選択行と比較範囲バーが再表示される。ラベルは「✕ 閉じる」に切り替わる。
- 比較を再実行(runDiff)した瞬間に `show-controls` は解除され、編集ボタンは「✎ 編集」に戻る。

### 14.5 タッチ操作 — ステージのパン・ピンチ・ダブルタップ
デスクトップ用のマウスイベント(mousedown/move/up + wheel)に加え、モバイル用に touch イベントを追加する。

| 操作 | 動作 |
|---|---|
| 1本指ドラッグ | パン(移動) |
| 2本指ピンチ | ズーム(中心点を維持) |
| ダブルタップ(300ms以内) | FIT にリセット |
| ピンチ→片手リリース | パンに自動引き継ぎ |

- `touchstart` で2本指を検知したらピンチモードに移行、距離変化率で `zoomAt(factor, cx, cy)` を呼ぶ。
- 比較済みステージは `touch-action: none` を指定し、ブラウザの既定ジェスチャ(ページスクロール等)と衝突しないようにする。

### 14.6 索引項目クリック時の中心合わせ — Overlay モードのバグ修正
従来は `getBoundingClientRect()` で SVG `<rect>` の画面位置を取得して view を補正していたが、Overlay モードでは `.pageWrap` が `position: relative` になり、ブラウザによっては親 transform を反映しない BCR を返すケースがあった(PDF が画面外に飛ぶ)。

修正後はレイアウト情報のみから直接計算する。

```
rect の中心(CSSピクセル, transform前)
= frame.offsetLeft + (rectAttrX + rectAttrW/2) × (frame.clientWidth / nativeW)
view.x = stage.clientWidth / 2 − 中心x × view.scale
```

これにより SBS / Overlay / DIFF のいずれでも同じロジックで動く。BCR の親 transform 相互作用には依存しない。

### 14.7 サンプル読み込みの file:// 対応
サンプルPDFを `fetch('samples/*.pdf')` で取得していたが、`file://` で開いた場合に CORS で失敗していた(エラー: 「ローカルサーバーで開いてください」)。

対策として、サンプルPDFを Base64 化した `samples/samples_data.js` (約 354KB) を別ファイルに切り出し、サンプルボタン押下時に `<script>` タグを動的注入して `window.SAMPLE_PDFS` を読み込む方式に変更した。`atob` → `Uint8Array.buffer` で ArrayBuffer 化し、`loadPdf()` に渡す。

- 初回クリックまでロードされないため、サンプルを使わないユーザーには配信コストがかからない。
- `<script>` タグは file:// でも同階層の JS を読めるため、ローカルサーバー不要。

### 14.8 その他のモバイル微調整
- 空状態(`body:not(.has-pages)`)では `#stage` を `overflow-y: auto` にして使い方説明が長くてもスクロール可能にする。`#empty` は `position: relative` でフローに戻し、`#stage::after`(透かし装飾) を非表示にしてスクロール領域を汚さない。
- ファイル選択スロットを縦に積み、`.file-row .swap` を上下区切り表示に変更。
- 出力ボタン群(`.header-actions`)はモバイル比較済み時に `flex-wrap: wrap` + 自然幅で配置、フォントとパディングを圧縮。
- `#viewHint`(マウス操作ヒント)、`#renderInfo`(解像度表示)、`.zoom-control .tool-eye` ラベル等はモバイルで非表示。
- `body { height: 100dvh }` を追加してアドレスバー伸縮に追従。

### 14.9 受入基準(追加分)
- iPhone(幅 ≤ 480px) / Android標準サイズ(幅 ≤ 820px) で全機能が操作可能。
- 比較完了後、PDFビュー領域が画面の半分以上を占める。
- 「✎ 編集」ボタンで範囲・ファイル変更にアクセスでき、再比較で自動的に閉じる。
- ステージ上で1本指パン・2本指ピンチ・ダブルタップFITが動く。
- サンプルボタンが `file://` 直開きでも動作する。
- Overlay モードで索引から選択した差分が画面中央に表示される。
- 空状態の使い方説明が縦スクロールで全文読める。

### 14.10 影響範囲(ファイル)
| 改修対象 | 内容 |
|---|---|
| `index.html` | ヘッダに「✎ 編集」ボタン追加。`<aside id="sidebar">` で索引にID付与。 |
| `pdfDiff.css` | `@media (max-width: 820px)` と `@media (max-width: 480px)` のレスポンシブ実装。`body.has-pages` / `body.show-controls` の状態スタイル。 |
| `pdfDiff.js` | タッチイベントハンドラ。`document.body.classList.add('has-pages')`(runDiff時)。編集モードトグル。`focusDiff` の中心計算を offsetLeft ベースに刷新。`loadSample` を Base64 遅延ロード方式に変更。 |
| `samples/samples_data.js` (新規) | 4つのサンプルPDFを Base64 化して `window.SAMPLE_PDFS` に格納。 |
